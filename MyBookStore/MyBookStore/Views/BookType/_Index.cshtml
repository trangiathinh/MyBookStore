@model List<MyBookStore.Models.BookType>
    <div class="category col-lg-3">
        <strong>@ViewBag.Title</strong>
        <hr />
        @{
            if (Model.Count > 0)
            {
                <nav class="navbar">
                            <ul class="navbar-nav ml-0 pl-0 nav-pills" id="left-category">
                                @foreach (var item in Model)
                                {
                                    <!-- Links nav-item nav-link-->
                                <li class="@(item.Id==ViewBag.BookTypeId?" active":"")">
                                    <a class="" href="@Url.Action("Index","Book",new { bookTypeId=item.Id})">@item.BookTypeName</a>
                                    <hr />
                                </li>
                                    
                                    @*<li class="nav-item">
                <a class="nav-link" href="#">Sách kinh tế - tài chính</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Sách giáo khoa</a>
            </li>*@
                                }
                            </ul>
                </nav>
            }
        }
        
    </div>
